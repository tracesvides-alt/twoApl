# 検証メモ

このファイルは、GitHubへ公開する前に確認する項目をまとめたものです。

## 自動チェック

```bash
npm run typecheck
npm run lint
npm test
npm run build
```

現在のローカル環境では、上記4項目が成功しています。

テストでは次の内容を確認します。

- 車を走らせる、動物を探す、荷物を運ぶ、の3ループ
- スター獲得と車・動物・庭アイテムのアンロック
- 不正な操作値を受け取った場合の状態維持
- 進行状況と設定の保存、バックアップ、読み込み
- 庭アイテムのドラッグ移動、配置の保存、旧データからの移行
- 保護者設定からのリセット
- Service Workerのキャッシュ対象とオフラインフォールバック

## 手動チェック

開発サーバーまたは `npm run preview` を起動し、スマートフォンまたはタブレットで次を確認します。

1. トップ画面のスタートボタンをタップできる
2. 車の選択とあそびの選択が指で操作できる
3. 画面のヒント表示で、次にタップする場所が分かる
4. 連打や少し外れたタップで画面が壊れない
5. 成功時に音、光、動きが発生する
6. あそびの完了後にスターと新しいアイテムが増える
7. 庭の車・動物・アイテムをドラッグして移動できる
8. 画面を再読み込みしても庭の配置と進行状況が残る
9. 保護者設定でBGM、効果音、振動、ヒント表示を切り替えられる
10. リセット確認後に初期状態へ戻せる
11. ブラウザのホーム画面への追加またはインストールができる
12. 通信を切った状態でも、インストール後の画面を起動できる

## 対応方針

- タップ領域は幼児の指で押しやすい大きさにする
- ドラッグ対象は指で隠れても位置が分かるようにする
- 失敗や時間制限でゲームオーバーにしない
- 文字を読まなくても、アイコン、色、動き、音で次の操作を示す
- 音と振動は保護者設定から停止できるようにする

実機での操作感は、画面サイズ、ブラウザ、OS、音量設定によって変わるため、リリース前に実際の端末でも確認します。
