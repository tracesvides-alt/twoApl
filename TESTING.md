# 検証記録

実施日：2026-09-06。Windows / Node.js、Chromium系のアプリ内ブラウザ。

## 自動検証

- `npm run typecheck`：TypeScriptの型検査。
- `npm run lint`：アプリ・モデル・スクリプト・テストのLint。
- `npm test`：10件。3種類のゲームループ、重複報酬防止、途中再開、24回分の成長、保存とバックアップ、庭の位置計算と保存、旧データ移行、リセットの範囲と失敗、Service Workerのオフライン応答。
- `npm run build`：ゲームと保護者設定ページの静的出力、PWAの全アセット事前キャッシュを生成。

Service WorkerのテストはNodeの分離したVMと通信失敗のモックで実行します。トップページと `/parents/` がオフライン時に正しいキャッシュを返すこと、別オリジンへのリクエストに介入しないこと、他アプリのキャッシュを削除しないことを確認します。実際のブラウザでのオフライン起動を代替したとは扱っていません。

## ブラウザで確認済み

以下は庭の自由移動・専用設定ページ・新しい指マークを追加する前の版で実施しました。

- 390×844のスマートフォン幅で出発、3回の移動、果物のタップ収集。
- 果物を1つ収集後に再読み込みし、1/3から復帰。
- 果物をかごへドラッグし、2/3に進むこと。
- 果物収集を完了し、おみやげを受け取り、庭に花が増える一巡。
- 1024×768のタブレット幅で洗車の5つの汚れを落とし、報酬のきつねを獲得。
- 新しいきつねを選んで出発し、3つの花へ2回ずつ水をやってトラックを獲得。
- 庭の物をタップして反応させ、再度お出かけできること。
- 3D玩具風の新アートとネイビーの操作盤を、スマートフォン幅・タブレット幅で表示確認。
- WebMCPの `read_garden_progress` が進み具合を返し、余計な入力を意図どおり拒否すること。

## 残る実機・ブラウザ確認

本番プレビューへのブラウザ遷移が、自動承認レビューの利用上限により拒否されました。拒否後はブラウザ操作を継続していません。

そのため、最新追加の庭の自由移動、専用設定ページのゲートとリセット、指マーク、短い画面や横向きの最終調整は、型検査・Lint・状態と計算のテスト・ビルドまでの検証です。これらの最新UIを実ブラウザで操作したとは主張しません。本番PWAのブラウザでのオフライン起動も未確認です。

iPhone/iPad/Androidの実機タッチ、ホーム画面へのインストール、Safariの音声開始、実際の2歳児による遊びやすさは未検証です。

再開時の確認手順：

1. `npm run build` → `npm run preview` で本番画面を開く。
2. スマホ幅とタブレット幅で庭のアイテムを動かし、枠外ドロップ、再読み込み、端末の縦横変更でも配置が保たれることを確認。
3. 保護者設定で数字を誤順に押すと入れず、正順で入れることを確認。
4. リセットの文字が不一致なら実行できず、キャンセルで進み具合が変わらないことを確認。全リセットの実行は使い捨てのテスト用ブラウザ記録で行う。
5. 各ゲームで最初の指マークが表示され、成功すると次の対象に移ることを確認。
6. PWAの事前キャッシュが完了した後、静的プレビューサーバーを停止し、再読み込み・設定ページ・一巡・保存が動作することを確認。
